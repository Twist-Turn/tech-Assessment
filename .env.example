# MongoDB Atlas connection string (with username, password, and cluster host).
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=rengy

# Server-side JWT signing secret. Generate one with: openssl rand -base64 32
JWT_SECRET=replace-with-32+-bytes-of-random
